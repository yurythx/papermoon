"""
Placeholder — IsActiveCustomer permission is tested in
tests/unit/test_is_active_customer_permission.py (direct unit tests).

All API views in this project explicitly set permission_classes = [IsAuthenticated],
overriding the default. The permission class is exercised directly to avoid
false-negative assumptions about which views use the default permission stack.
"""
