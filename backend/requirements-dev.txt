# Dev / CI only — not installed in production image
ruff==0.5.7
mypy==1.11.0
django-stubs==5.0.4
djangorestframework-stubs==3.15.0
mypy-extensions==1.0.0
pytest-cov==5.0.0
# pre-commit itself runs in a venv, but list here so `make install` picks it up
pre-commit==3.8.0
