# Dev / CI only — not installed in production image
#
# mypy/django-stubs/djangorestframework-stubs NÃO ficam pinados aqui: já
# estão em requirements.txt (instalado antes deste no CI, via
# `pip install -r requirements.txt -r requirements-dev.txt`), e um pin
# duplicado aqui só desalinha na próxima vez que alguém atualizar a versão
# só no requirements.txt — foi exatamente o que quebrou o CI (mypy
# 1.11.2 vs 1.11.0, djangorestframework-stubs 3.15.1 vs 3.15.0).
ruff==0.5.7
mypy-extensions==1.0.0
pytest-cov==5.0.0
# pre-commit itself runs in a venv, but list here so `make install` picks it up
pre-commit==3.8.0
